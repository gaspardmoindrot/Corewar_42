#include "../../includes/dasm.h"

int	write_reg(t_dasm *dasm, int j, int i)
{
	int	nb;

	nb = dasm->buf[i + 1];

}

int	write_dir(t_dasm *dasm, int j, int i)
{
}

int	write_ind(t_dasm *dasm, int j, int i)
{
}
